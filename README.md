# WordPress Headless Starter

A dependency-free ESM client for WordPress REST API content queries.

```bash
WP_URL=https://example.com npm run example
```

Use the client as a small, testable data layer for a Next.js, Astro, or custom frontend.
